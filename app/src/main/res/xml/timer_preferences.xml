<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="enable_sound"
        android:title="Enable sound"
        android:summaryOff="Sound disable"
        android:summaryOn="Sound enable"
        />

    <ListPreference
        android:title="Timer melody"
        android:key="timer_melody"
        android:defaultValue="bell"
        android:entries="@array/pef_timer_melody_label"
        android:entryValues="@array/pef_timer_melody_value"
        />

    <EditTextPreference
        android:title="Default timer"
        android:key="default_timer"
        android:defaultValue="30"
        />

</PreferenceScreen>